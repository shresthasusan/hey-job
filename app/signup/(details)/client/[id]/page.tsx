import ClientForm from "@/app/ui/login-signup-component/client/clientForms";

const page = () => {
  return (
    <div className="flex items-center justify-center  h-screen">
      <ClientForm />
    </div>
  );
};

export default page;
